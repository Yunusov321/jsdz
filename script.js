let ot = prompt("Ismingizni kiriting")

let space = prompt("space di idini kiriting")

let password = prompt("space dagi passwordingizni kiriting")

let all = {
    name: `${ot}`,
    id: `${space}`,
    password: `${password}`
};

console.log(all);